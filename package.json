{
  "name": "ecommerce-platform",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:store": "turbo dev --filter=store",
    "dev:admin": "turbo dev --filter=admin-hub",
    "build:store": "turbo build --filter=store",
    "test": "turbo test"
  },
  "devDependencies": {
    "next": "^15.5.22",
    "turbo": "^2.10.7",
    "typescript": "^5"
  },
  "overrides": {
    "next": {
      "postcss": "^8.5.10",
      "sharp": "^0.35.0"
    },
    "next-auth": {
      "uuid": "^11.1.1",
      "@auth/core": {
        ".": "^0.41.3",
        "cookie": "^0.7.2"
      }
    },
    "@auth/core": "^0.41.3"
  }
}
